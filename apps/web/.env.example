# Copy to `.env.local` for local overrides. Never commit `.env` or `.env.local`.
#
# Optional: future REST backend (not required today — AI runs in the browser).
# Production must be HTTPS. Do not point this at localhost when deploying.
# VITE_API_BASE_URL=https://api.example.com
